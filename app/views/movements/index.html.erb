<h1>Movimientos</h1>

<table>
  <thead>
    <tr>
      <th>Bolsillo</th>
      <th>Fecha</th>
      <th>Descripción</th>
      <th>Monto</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @movements.each do |movement| %>
      <tr>
        <td><%= movement.pocket_id %></td>
        <td><%= movement.created_at.strftime("%d/%m/%Y") %></td>
        <td><%= movement.description %></td>
        <td><%= number_to_currency movement.amount, delimiter: ".", precision: 0 %></td>
        <td><%= link_to 'Show', movement %></td>
        <td><%= link_to 'Edit', edit_movement_path(movement) %></td>
        <td><%= link_to 'Destroy', movement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Movement', new_movement_path %>
